
容器内工具安装
```sh
    apt-get update
    apt-get install iputils-ping            # for ping
    apt-get install net-tools               # for ifconfig
    apt-get install telnet                  # for telent
```
